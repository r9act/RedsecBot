# 🔴 RedSec Discord Bot

Discord-бот для отслеживания и анализа статистики **Battlefield 6 (REDSEC / BR)**  
с интеграцией **Tracker.gg**, хранением истории в **Cassandra** и пользовательских маппингов в **PostgreSQL**.

---

## ✨ Возможности

- 🔗 Привязка Battlefield-профиля к Discord-аккаунту
- 📊 Просмотр REDSEC-статистики игрока
- 🔍 Поиск игроков по нику (с выбором, если найдено несколько)
- 🕓 Кэширование и история статистики
- 🏆 Отображение карьерного ранга (rank / name / image)
- 🧩 Расширяемая архитектура (AOP, генерация изображений, новые источники)

---

## ⚙️ Технологии

- Java 21  
- Spring Boot 3  
- JDA 5 (Discord API)  
- WebClient  
- PostgreSQL 16  
- Cassandra 4.1  
- Docker / Docker Compose  
- Liquibase (только для PostgreSQL)

---

## 🗄️ Хранилища данных

### PostgreSQL

Используется для хранения пользовательских маппингов Discord ↔ Battlefield.

- Управляется через **Liquibase**
- Таблица: `discord_user_mapping`

---

### Cassandra

Используется для хранения **истории статистики игроков**.

```sql
CREATE TABLE player_stats_history (
    player_key text,
    stats_type text,
    fetched_at timestamp,
    payload text,
    source text,
    PRIMARY KEY ((player_key, stats_type), fetched_at)
) WITH CLUSTERING ORDER BY (fetched_at DESC);
